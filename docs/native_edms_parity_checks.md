# Native EDMS Parity Checks

- Count parity for projects, organizations and users
- Count parity for archive manifest rows
- Duplicate key check on `doc_number + revision + file_kind`
- Random file hash spot-check before cutover sign-off
